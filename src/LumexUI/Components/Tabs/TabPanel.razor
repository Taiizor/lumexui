@namespace LumexUI.Internal
@inherits LumexComponentBase

@if( SelectedTab?.ChildContent is not null )
{
    <LumexComponent As="@As"
                    Class="@Slots.TabPanel"
                    data-slot="tab-panel">
        @SelectedTab?.ChildContent
    </LumexComponent>
}
